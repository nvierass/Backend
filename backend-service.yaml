apiVersion: apps/v1
Kind: Service
metadata:
	name: backend-service
spec:
  type: Loadbalancer
  selector:
  app:backend
  ports:
  -name:http
  protocol:TCP
  port:8080
  targetPort:8080
  nodePort:30000