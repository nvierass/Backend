 apiVersion: apps/v1
 Kind: Service
 metadata:
   name: backend-service
 spec:
  - type: Loadbalancer
    selector:
     app:backend
    ports:
     - name:http
       protocol:TCP
       port:32000
       targetPort:32000
       nodePort:30000
