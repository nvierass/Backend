apiVersion: apps/v1
Kind: Service
metadata:
	name: backend-service
spec:
  type: Loadbalancer
  selector:
    app:backend
  ports:
  - name:http
    protocol:TCP
    port:51000
    targetPort:50001
    nodePort:51002